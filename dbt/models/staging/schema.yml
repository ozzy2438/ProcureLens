version: 2

models:
  - name: stg_contract_notices
    description: Typed, one-row-per-release view over raw OCDS contract notices.
    columns:
      - name: release_id
        tests: [not_null, unique]
      - name: ocid
        tests: [not_null]
      - name: is_amendment
        tests: [not_null]
