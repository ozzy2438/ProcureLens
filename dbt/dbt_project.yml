name: procurelens
version: "1.0.0"
profile: procurelens

model-paths: ["models"]
test-paths: ["tests"]

models:
  procurelens:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
