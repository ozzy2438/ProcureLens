procurelens:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DBT_HOST', 'localhost') }}"
      port: "{{ env_var('DBT_PORT', '5432') | int }}"
      user: "{{ env_var('DBT_USER', 'procurelens') }}"
      password: "{{ env_var('DBT_PASSWORD', 'procurelens') }}"
      dbname: "{{ env_var('DBT_DATABASE', 'procurelens') }}"
      schema: analytics
      threads: 4
