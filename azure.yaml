name: procurelens
metadata:
  template: procurelens@1.0.0
services:
  api:
    project: .
    host: containerapp
    language: python
    docker:
      path: docker/Dockerfile.api
      context: .
  ui:
    project: .
    host: containerapp
    language: python
    docker:
      path: docker/Dockerfile.ui
      context: .
